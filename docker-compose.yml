version: "3.9"

services:
  api:
    build: .
    container_name: finance_api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8989:8000"
    command: >
      uvicorn app.main:app --host 0.0.0.0 --port 8000